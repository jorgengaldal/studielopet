---
import LinkButton from './LinkButton.astro'


// interface Props {
//     years: number[]
// }

// const {years} = Astro.props
---

<form class="flex flex-row gap-5 items-center">
    <div class="flex flex-col items-center">
        <label for="year" class="font-thin">År</label>
        <select name="year" id="year" class="bg-dark rounded-sm">
            <!-- {years.toSorted((a, b) => b - a).map((year) => {
                return <option value={year}>{year}</option>
            })} -->
    </select>
</div>
    <LinkButton>Bekreft filter</LinkButton>
</form>

<style>
    input[type="range"] {
        writing-mode: vertical-lr;
        direction: rtl;
    }
</style>

<script>
    const yearResult = document.getElementById("yearResult")
    document.getElementById("yearRange")?.addEventListener("input", (e) => {
        if (!yearResult) return
        yearResult!.textContent = (e.target as HTMLInputElement).value
    }

    )
</script>